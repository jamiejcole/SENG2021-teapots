info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: https://api.orderms.tech/v1/auth/login
  body:
    type: json
    data: |-
      {
        "email": "hello2world22222@example.com",
        "password": "$$$$Password123!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
