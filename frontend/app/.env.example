# Use /api/v2 for the web app (JWT auth + protected routes). /api/v1 is API-key-only (no /auth).
VITE_API_BASE_URL=http://localhost:3002/api/v2
# Must match BACKEND_API_KEY in backend/.env
VITE_API_KEY=dev-local-key
