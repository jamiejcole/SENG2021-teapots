/**
 * Re-exports the custom Select (floating panel, keyboard, optional search).
 * Prefer importing from `@/components/ui/select`.
 */
export { Select } from './select'
export type { SelectOption, SelectBadge, SelectProps } from './select'
